import styled from "styled-components";
import { red } from '@mui/material/colors';

export const ZoomEnabledNote = styled.div`
    border-top: 1px solid #000000;
    padding-top: 5px;
    color: ${red[500]}
`;
